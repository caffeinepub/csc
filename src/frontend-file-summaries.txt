{
  "frontend/src/App.tsx": "Main application component with updated homepage flow including new MissionVisionSection and FAQSection between hero and inquiry form, preserving admin route guard and error boundary.",
  "frontend/src/content/siteContent.ts": "Centralized content store containing all new Hindi business copy organized into 6 detailed service departments, mission/vision text, 4 FAQs, whyChooseUs reasons, full contact details including email and opening hours, and updated service categories for the inquiry form dropdown.",
  "frontend/src/hooks/useQueries.ts": "React Query mutation hook for public inquiry submission without admin cache invalidation.",
  "frontend/src/components/BrandMark.tsx": "Reusable logo component that displays the generated Vaishnavi logo image with configurable sizes.",
  "frontend/src/components/NavigationBar.tsx": "Navigation bar updated with links to all new sections including mission-vision and FAQ anchors, maintaining smooth scroll functionality and mobile menu.",
  "frontend/src/components/sections/HeroSection.tsx": "Hero section displaying the new Hindi tagline, subtitle, welcome message, intro paragraph, and existing CTAs (phone/WhatsApp/Official Login) with updated spacing for long-form content.",
  "frontend/src/components/sections/ServicesSection.tsx": "Redesigned services section displaying exactly 6 departments with accordion-based expandable items showing full bullet lists, mobile-friendly stacked layout and desktop-optimized presentation.",
  "frontend/src/components/sections/WhyChooseUsSection.tsx": "Section showcasing five key reasons to choose the business using icon cards in a responsive grid.",
  "frontend/src/components/InquiryForm.tsx": "Controlled form component with validation for inquiry submission that shows success feedback, resets form after successful submission, handles errors gracefully, and correctly maps serviceCategories as strings for the service category dropdown.",
  "frontend/src/components/sections/FormSection.tsx": "Wrapper section for the inquiry form with updated spacing to integrate into the new long-form homepage layout.",
  "frontend/src/components/sections/ContactSection.tsx": "Updated contact section displaying full multi-line address with pincode, email address, opening hours, operator details, and functional phone/WhatsApp action buttons.",
  "frontend/src/components/Footer.tsx": "Footer component updated to show full address with pincode, email, and additional copyright tagline while preserving Caffeine.ai attribution with UTM tracking.",
  "frontend/src/index.css": "Global styles with warm neutral OKLCH color system featuring saffron primary and green accent colors for both light and dark modes.",
  "frontend/tailwind.config.js": "Tailwind configuration extending the theme with custom color tokens, border radius, shadows, and animations.",
  "frontend/src/pages/AdminPage.tsx": "Admin dashboard page with Official Login session guard that displays explicit initialization, ready, and failed states for admin-actor initialization, detects and handles \"Admin secret already used\" as recoverable with Retry as primary action, shows actionable error UI with replica rejection details included in the primary visible message, provides Retry button that resets UI state and triggers fresh initialization with automatic bounded retries, separates initialization errors from inquiry-fetching errors without auto-retry loops, and displays all UI strings in English.",
  "frontend/src/components/admin/AdminLoginPrompt.tsx": "Internet Identity login/logout component that now supports optional redirect after successful login via redirectAfterLogin prop.",
  "frontend/src/components/admin/InquiryList.tsx": "Displays inquiries in a filterable tabbed list (all/unread/read) with search by name/phone, filters by inquiry type and service category, bulk actions for marking multiple inquiries as read/unread, cards showing all inquiry details with inline actions, and all UI strings in English with en-US locale formatting.",
  "frontend/src/components/admin/InquiryActions.tsx": "Provides dropdown menus for individual inquiry actions (mark read/unread, delete with confirmation) and bulk export actions (JSON/CSV) with loading states, English labels, and toast notifications.",
  "frontend/src/utils/inquiryExport.ts": "Utility functions to export inquiry data as downloadable JSON or CSV files with proper formatting, escaping, English labels, and en-US locale timestamp formatting.",
  "frontend/src/hooks/useAdminInquiries.ts": "React Query hooks for admin operations that expose admin-actor initialization states, gate inquiry fetching on isReady state AND actor availability AND not-fetching, include limited self-healing mechanism that automatically reinitializes admin session and retries ONCE per query lifecycle if authorization error occurs (preventing infinite loops), provide safeRefetch with user-friendly toast messages, provide useBulkSetInquiryReadStatus hook for bulk operations, and use English toast messages for all mutations with proper prerequisite guards.",
  "frontend/src/main.tsx": "Application entry point that sets up React Query, theme provider, and toast notifications (Sonner) for the entire app.",
  "frontend/src/hooks/useInternetIdentity.ts": "Internet Identity authentication provider that loads the identity provider URL from config at runtime instead of using process.env, ensuring production build compatibility.",
  "frontend/src/hooks/useActor.ts": "Backend actor initialization hook that reads admin token from both sessionStorage (Official Login) and hash fragments, includes token in query key for proper actor recreation, listens to officialLoginChange events to refetch actor, and initializes access control for both anonymous and authenticated actors.",
  "frontend/public/admin/index.html": "Static HTML entry point for /admin route that loads the SPA without redirect, allowing app-level routing guard to control access.",
  "frontend/public/404.html": "SPA fallback page that preserves the originally requested path in sessionStorage before redirecting to root, enabling client-side routing to restore deep links in production with error handling.",
  "frontend/src/components/sections/AdminLoginSection.tsx": "New dedicated admin login section with collapsible Admin Access button that reveals Internet Identity login UI when expanded.",
  "frontend/src/hooks/useOfficialLogin.ts": "Custom hook managing Official Login session state with deterministic token-first storage order, admin token persistence in sessionStorage using the correct backend secret token (admin-secret-token-2024), credential validation, and same-tab event notifications for login/logout state changes with deferred event dispatch to ensure storage writes complete.",
  "frontend/src/components/admin/OfficialLoginDialog.tsx": "Dialog component for Official Login with User ID and Password inputs, validation against exact credentials, error handling with English messages, loading states, and success callback to navigate to admin dashboard.",
  "frontend/src/utils/navigation.ts": "SPA navigation helper that updates browser history and dispatches popstate events to notify routing logic without full page reloads.",
  "frontend/PRODUCTION_SMOKE_TEST.md": "Comprehensive production smoke-test checklist with strengthened Prerequisites section that explicitly requires verifying the backend canister is running before testing Official Login and /admin, includes step-by-step commands to check canister status and start stopped canisters, documents automatic retry with exponential backoff (up to 3 attempts over ~14 seconds), provides detailed troubleshooting for \"Admin secret already used\" (documented as expected behavior with Retry as primary action), includes dedicated test for backend canister stopped scenario with clear resolution steps, and documents expected behavior for all error recovery patterns (English only).",
  "frontend/src/components/ErrorBoundary.tsx": "React error boundary component that catches runtime errors and displays a user-friendly fallback UI in English with a reset button to return to the homepage, preventing blank screens on unexpected errors.",
  "frontend/src/hooks/useOfficialAdminToken.ts": "Custom hook that reads the admin secret token from sessionStorage, re-reads on mount to handle navigation after login, and stays in sync within the same tab by listening to officialLoginChange events, enabling downstream actor creation to always have the latest token.",
  "frontend/src/hooks/useAdminActor.ts": "Admin actor hook that creates an actor with access-control initialization using the admin secret token, explicitly verifies admin status after initialization to prevent infinite loops, treats \"Admin secret already used\" as expected and non-blocking, implements bounded retry with exponential backoff for replica rejection errors, includes 30-second timeout, and exposes explicit states (isInitializing, isReady, isError) with deterministic retry that clears both actor and inquiry caches.",
  "frontend/src/utils/adminError.ts": "Utility functions to extract error messages, detect authorization errors (excluding \"admin secret already used\" but including \"does not have admin privileges\"), detect recoverable errors (including \"admin secret already used\" and replica rejections), identify replica rejection errors, extract replica rejection details (reject code, request ID, canister stopped status), and format user-friendly, actionable messages that include reject code and request ID inline in the primary message for admin-session initialization failures, with all admin init/error messages in English.",
  "frontend/PREVIEW_VALIDATION_CHECKLIST.md": "Comprehensive preview environment validation checklist with complete Prerequisites section documenting how to verify the backend canister is running before testing Official Login and /admin, including step-by-step commands to check canister status, start stopped canisters, test backend connectivity, detailed troubleshooting for common initialization errors including \"Admin secret already used\" (documented as expected behavior with Retry as the solution), explanation of automatic retry with exponential backoff (up to 3 attempts over ~14 seconds), and dedicated section explaining error recovery patterns (English only).",
  "frontend/index.html": "HTML entry point with updated document title matching the exact provided Hindi title string including emoji.",
  "frontend/src/components/sections/MissionVisionSection.tsx": "New homepage section component rendering the mission and vision Hindi text in a card layout with icon and proper spacing.",
  "frontend/src/components/sections/FAQSection.tsx": "New FAQ section component rendering the 4 provided Hindi Q&A pairs in an accordion layout with proper styling and section anchor for navigation.",
  "frontend/src/backend.d.ts": "TypeScript interface definitions for the backend canister including the _initializeAccessControlWithSecret method for admin session initialization, all inquiry management methods, user profile methods, and role-based access control methods."
}